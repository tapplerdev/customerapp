import { StyleSheet } from "react-native"

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#fff",
    alignItems: "center",
    justifyContent: "center",
  },
  lottieSize: { width: 250, height: 250 },
  logoPosition: { position: "absolute", bottom: 60 },
})
