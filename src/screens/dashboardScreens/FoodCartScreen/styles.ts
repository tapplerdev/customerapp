import { StyleSheet } from "react-native"

const THUMB = 88

const styles = StyleSheet.create({
  rightCol: {
    width: THUMB,
  },
  thumb: {
    width: THUMB,
    height: THUMB,
  },
  stepper: {
    width: THUMB,
  },
})

export default styles
