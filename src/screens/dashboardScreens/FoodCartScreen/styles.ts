import { StyleSheet } from "react-native"

const THUMB = 88

const styles = StyleSheet.create({
  thumb: {
    width: THUMB,
    height: THUMB,
  },
  stepper: {
    width: 104,
  },
})

export default styles
