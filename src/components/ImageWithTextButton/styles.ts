import { StyleSheet } from "react-native"

export default StyleSheet.create({
  borderBase: {
    borderWidth: 0.75,
    borderRadius: 10,
    overflow: "hidden",
  },
  scaledImage: { transform: [{ scale: 0.7 }] },
})
