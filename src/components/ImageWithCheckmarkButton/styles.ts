import { StyleSheet } from "react-native"

export default StyleSheet.create({
  borderBase: {
    borderWidth: 0.75,
    borderRadius: 10,
  },
  checkmarkText: { color: "#CC0000", fontSize: 16, fontWeight: "bold" },
  imageContainer: {
    width: 100,
    alignItems: "flex-start",
    justifyContent: "center",
  },
})
